version: 2

models:
  - name: stg_dremio_pscore_sgw
    description: raw data for data of the pscore network element
    tags:
      - pscore
      - bronze
    columns:
      - name: imsi
        tests:
          - not_null
      - name: msisdn
        tests:
          - not_null